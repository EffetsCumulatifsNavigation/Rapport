# Accronymes

<!-- Faire une base de données -->

```{r}
  accr <- read.table('./Data/accronymes.csv', sep = '\t', header = TRUE, stringsAsFactors = FALSE, quote = "\"")

  library(knitr)
  library(kableExtra)
  accr[order(accr$Accronymes), ] %>%
  kbl(caption = "Liste des accronymes", row.names = FALSE) %>%
  kable_classic(full_width = F, html_font = "Cambria")
```
